(()=>{
  const confirmar = confirm( "Queres?")

  if(confirmar){
    // bloque de código
    // local del if
    let respuesta = "Si quiero"
    alert(respuesta)
    {
      console.log("bloque dentro del if", respuesta)
      let test = "probando"
      // var feo = "No debería existir"
    }
    // console.log(test)
  }
})()
// console.log(respuesta) // Esta fuera del scope


{
  // console.log(respuesta)
  // console.log(feo)
  // var feo = "Muy feo"
  // console.log(feo)
}